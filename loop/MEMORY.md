# MEMORY — ループの記憶（背骨）

> エージェントは毎周このファイルを読み、末尾に追記する。会話履歴ではなく**このファイルが記憶**。
> 人間が消さない限り、後の周でも過去の試行を踏まえて動ける。
>
> オペレータ（人間）は、方向修正したいとき Open の**最上部に `[operator/...]` 注記**を差し込む。
> エージェントはそれを最優先の現状認識として扱う。

## Done（達成済み）

<!-- 例:
- [task-x] 〜を実装。〜の理由でこう設計した。落とし穴: 〜。VERIFY グリーン。
-->

## Open（未解決 / 次周への申し送り）

- [blocked] issue #3「codex対応」: 受け入れ条件が曖昧（対象フォーマット・スコープ未確定）かつ
  設計判断（skillのパッケージ/ロード方式をcodex/opencodeにも対応させる方針）を要するため、
  `loop-needs-human` を付与しコメントで理由を残した。人間が仕様/スコープを確定してから
  再度 `auto-fix` として着手可能にする想定。

## Notes（学び / 落とし穴）

<!-- 例:
- VERIFY は 〜 だけでなく 〜 も必要（〜で落ちる）。
- 正解は 〜 の挙動。移植/変更時は対応する 〜 を読んで仕様を合わせる。
-->
